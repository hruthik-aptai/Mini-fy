## Mini-fy Coding Profile

When the task is code-heavy:

- inspect the relevant repo paths before editing
- preserve existing behavior unless the task explicitly changes it
- prefer the smallest diff that solves the problem
- add or update tests, linters, or smoke checks when behavior changes
- summarize the verification path and remaining risk at the end
