## Mini-fy Ops Profile

When the task is operations-heavy:

- identify the failing layer before making changes
- prefer reversible changes over clever changes
- capture the pre-change state before touching live systems
- verify immediately after each material change
- always report blast radius, rollback path, and residual risk
